name: Backend CI/CD (Private via Bastion)

on:
  push:
    branches: ["main"]

jobs:
  deploy-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy Backend using Bastion (Amazon Linux 2023 + RDS)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.BACKEND_PRIVATE_IP }}
          username: ${{ secrets.BACKEND_USER }}
          key: ${{ secrets.BACKEND_KEY }}

          # Bastion settings
          proxy_host: ${{ secrets.BASTION_HOST }}
          proxy_username: ${{ secrets.BASTION_USER }}
          proxy_key: ${{ secrets.BASTION_KEY }}
          
          timeout: 60m
          script_stop: true

          script: |
            echo "🚀 Amazon Linux 2023 Backend + RDS deployment"
            set -e

            # ===== 1. MINIMAL DEPENDENCIES (Amazon Linux 2023) =====
            echo "📦 Installing AL2023 dependencies..."
            sudo dnf makecache
            sudo dnf install -y python3 python3-pip git mariadb105 || true
            echo "✅ Ready: git $(git --version), python $(python3 --version)"

            # ===== 2. CLEAN CLONE =====
            echo "🔄 Cloning repository..."
            REPO_DIR="/home/ec2-user/Paytm-fullstack-project"
            rm -rf "$REPO_DIR"
            git clone https://github.com/VaibhavJC/Paytm-fullstack-project.git "$REPO_DIR"
            cd "$REPO_DIR/Backend"
            echo "✅ Backend: $(pwd)"
            echo "📄 Files: $(ls -la *.sql 2>/dev/null || echo 'No .sql files')"

            # ===== 3. TRY RDS IMPORT (NON-BLOCKING) =====
            echo "☁️  RDS setup (optional)..."
            if command -v mysql >/dev/null 2>&1 && [ -f "paytm.sql" ]; then
              echo "🔗 Testing RDS: projectdb.c1uy4qas2jj8.ap-south-1.rds.amazonaws.com"
              timeout 10 mysql -h projectdb.c1uy4qas2jj8.ap-south-1.rds.amazonaws.com \
                -u admin -p'cloud123' -e "SE
